# Backend HTTP port
PORT=4000

# Optional Redis-backed execution queue
REDIS_URL=redis://localhost:6379
EXEC_CONCURRENCY=4

# Comma-separated browser origins allowed to call the API.
# Keep this aligned with your deployed frontend URL.
CORS_ORIGINS=http://localhost:3000
